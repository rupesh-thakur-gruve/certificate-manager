/**
 * App Component.
 * This file is kept for backwards compatibility.
 * Main routing is now handled in main.tsx with createBrowserRouter.
 */

import React from "react";

const App: React.FC = () => {
  // This component is not used anymore
  // Routing is handled directly in main.tsx with RouterProvider
  return null;
};

export default App;
